# Valete's take on an HomeLab dashboard because none where good enough

For the time being it's simple just:
`$ npm install `
`$ npm start`

## .env
UNSPLASH_ACCESS_KEY=Unsplash api access key

## Tech Stack

**Server:** Node, Express, ejs
